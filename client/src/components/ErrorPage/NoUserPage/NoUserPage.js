import React from 'react';

const NoUserPage = (props) => {
    // TODO: if isAuthenticated === false: make a link to signin
    return (
        <div>
            Requested User Page does not exist!
        </div>
    );
};

export default NoUserPage;